#ifndef CLEAN_H
#define CLEAN_H

/* Clean Types */

typedef int ColorPair;

struct Surface;
struct Element;

typedef struct Surface Surface;
typedef struct Element Element;

#include "surface.h"
#include "element.h"
#include "util.h"

#endif /* CLEAN_H */
