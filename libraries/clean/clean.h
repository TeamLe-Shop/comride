#ifndef CLEAN_H
#define CLEAN_H

/* Clean Types */

struct Surface;
struct Element;

typedef struct Surface Surface;
typedef struct Element Element;

#include "surface.h"
#include "element.h"

#endif /* CLEAN_H */
